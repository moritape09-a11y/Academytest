# 🔧 راهنمای حل مشکل: نمایش داده نشدن منوها

## ❌ مشکل: نمی‌توانم آموزشگاه/مدرسه/معلم اضافه کنم

این مشکل معمولاً به یکی از دلایل زیر است:

---

## ✅ راه‌حل 1: فعال‌سازی مجدد پلاگین

### مراحل:
1. به پنل وردپرس بروید
2. به **افزونه‌ها** بروید
3. پلاگین **"دایرکتوری آموزشی"** را پیدا کنید
4. روی **"غیرفعال"** کلیک کنید
5. صبر کنید 2 ثانیه
6. روی **"فعال‌سازی"** کلیک کنید
7. به صفحه پنل مدیریت بروید

### بررسی:
در منوی سمت راست باید ببینید:
- 🏢 آموزشگاه‌ها
- 🏫 مدارس
- 👨‍🏫 معلمین

---

## ✅ راه‌حل 2: بازنویسی Permalink

### مراحل:
1. به **تنظیمات** > **پیوندهای یکتا** بروید
2. هیچ تغییری ندهید
3. فقط روی دکمه **"ذخیره تغییرات"** کلیک کنید
4. به پنل اصلی برگردید

این کار rewrite rules را بازنویسی می‌کند.

---

## ✅ راه‌حل 3: بررسی مسیر فایل‌ها

### مطمئن شوید فایل‌ها در مسیر درست هستند:

```
wp-content/
└── plugins/
    └── educational-directory/
        ├── educational-directory.php  ← فایل اصلی
        ├── includes/
        │   ├── class-post-types.php
        │   ├── class-taxonomies.php
        │   ├── class-meta-boxes.php
        │   ├── class-shortcodes.php
        │   ├── class-templates.php
        │   └── class-search-filter.php
        └── assets/
            ├── css/
            ├── js/
            └── images/
```

**مهم:** نام پوشه باید دقیقاً `educational-directory` باشه.

---

## ✅ راه‌حل 4: بررسی خطاها

### فعال کردن Debug Mode:

در فایل `wp-config.php` این خطوط را اضافه کنید:

```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

سپس:
1. پلاگین را غیرفعال کنید
2. پلاگین را فعال کنید
3. فایل `wp-content/debug.log` را بررسی کنید

---

## ✅ راه‌حل 5: بررسی نسخه PHP

پلاگین نیاز به **PHP 7.2 یا بالاتر** دارد.

### بررسی نسخه PHP:
1. **ابزارها** > **سلامت سایت** بروید
2. بخش **اطلاعات** را باز کنید
3. نسخه PHP را بررسی کنید

اگر زیر 7.2 است، باید ارتقا دهید.

---

## ✅ راه‌حل 6: بررسی مجوزهای فایل

مجوزهای فایل باید این‌طور باشند:

```bash
# پوشه‌ها
chmod 755 educational-directory/
chmod 755 educational-directory/includes/
chmod 755 educational-directory/assets/

# فایل‌های PHP
chmod 644 educational-directory/*.php
chmod 644 educational-directory/includes/*.php
```

---

## ✅ راه‌حل 7: نصب از صفر

اگر هیچ کدام کار نکرد:

### مراحل:
1. پلاگین را **حذف کامل** کنید
2. پوشه `educational-directory` را از `wp-content/plugins/` پاک کنید
3. دوباره آپلود کنید
4. فعال کنید

---

## 🧪 تست پلاگین

برای اطمینان از اینکه پلاگین نصب شده، این کد را در `functions.php` تم بگذارید:

```php
// تست پلاگین دایرکتوری آموزشی
add_action('admin_notices', function() {
    if (function_exists('ED')) {
        echo '<div class="notice notice-success"><p>✅ پلاگین دایرکتوری آموزشی فعال است!</p></div>';
        
        // بررسی Post Types
        $post_types = get_post_types(array('_builtin' => false), 'names');
        if (in_array('academy', $post_types)) {
            echo '<div class="notice notice-success"><p>✅ آموزشگاه‌ها ثبت شده‌اند</p></div>';
        }
        if (in_array('school', $post_types)) {
            echo '<div class="notice notice-success"><p>✅ مدارس ثبت شده‌اند</p></div>';
        }
        if (in_array('teacher', $post_types)) {
            echo '<div class="notice notice-success"><p>✅ معلمین ثبت شده‌اند</p></div>';
        }
    } else {
        echo '<div class="notice notice-error"><p>❌ پلاگین دایرکتوری آموزشی فعال نیست!</p></div>';
    }
});
```

بعد از اضافه کردن این کد:
- به پنل مدیریت بروید
- پیام‌های بالای صفحه را بررسی کنید

---

## 🔍 بررسی دستی

### آیا پلاگین در لیست است؟

1. **افزونه‌ها** > **افزونه‌های نصب شده** بروید
2. دنبال **"دایرکتوری آموزشی"** بگردید
3. اگر نیست: فایل‌ها را دوباره آپلود کنید
4. اگر هست ولی غیرفعال است: فعالش کنید

---

## ⚠️ خطاهای رایج

### خطا 1: "The plugin does not have a valid header"
**علت:** فایل `educational-directory.php` در جای درست نیست  
**حل:** مطمئن شوید فایل در `wp-content/plugins/educational-directory/educational-directory.php` است

### خطا 2: "Plugin could not be activated"
**علت:** خطای PHP یا فایل‌های گم شده  
**حل:** Debug را فعال کنید و لاگ را بررسی کنید

### خطا 3: "Missing required files"
**علت:** فایل‌های `includes/` موجود نیستند  
**حل:** همه فایل‌ها را دوباره آپلود کنید

---

## 📞 اگر باز هم مشکل دارید

### اطلاعات زیر را به من بدهید:

1. **نسخه وردپرس:** تنظیمات > درباره وردپرس
2. **نسخه PHP:** ابزارها > سلامت سایت > اطلاعات
3. **خطای دریافتی:** (اگر هست)
4. **پیام پلاگین:** (هنگام فعال‌سازی)
5. **محتوای debug.log:** (اگر فعال کرده‌اید)

---

## ✅ چک لیست نهایی

قبل از اینکه بگویید کار نمی‌کند، لطفاً اینها را بررسی کنید:

- [ ] فایل‌ها در مسیر درست هستند
- [ ] نام پوشه `educational-directory` است
- [ ] پلاگین در لیست افزونه‌ها دیده می‌شود
- [ ] پلاگین فعال است (نه غیرفعال)
- [ ] Permalink بازنویسی شده (تنظیمات > پیوندهای یکتا > ذخیره)
- [ ] کش پاک شده (اگر از کش استفاده می‌کنید)
- [ ] PHP نسخه 7.2 یا بالاتر است
- [ ] وردپرس نسخه 5.0 یا بالاتر است

---

## 🚀 بعد از حل مشکل

وقتی مشکل حل شد، باید ببینید:

### در منوی پنل مدیریت:
```
پیشخوان
نوشته‌ها
رسانه‌ها
صفحات
نظرها
🏢 آموزشگاه‌ها         ← اینجا!
🏫 مدارس               ← اینجا!
👨‍🏫 معلمین              ← اینجا!
ظاهر
افزونه‌ها
کاربران
ابزارها
تنظیمات
```

اگر این منوها را می‌بینید، همه چیز درست است! ✅

---

**موفق باشید! 🎉**
