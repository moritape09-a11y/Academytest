# راهنمای نصب و پیکربندی

## مراحل نصب

### 1. نصب پلاگین

#### روش اول: نصب دستی
```bash
# دانلود فایل‌ها
cd /path/to/wordpress/wp-content/plugins/
git clone [repository-url] educational-directory
# یا کپی کردن فایل‌ها به صورت دستی
```

#### روش دوم: آپلود از پنل وردپرس
1. فایل‌ها را در یک فایل ZIP قرار دهید
2. به وردپرس > افزونه‌ها > افزودن افزونه بروید
3. روی "بارگذاری افزونه" کلیک کنید
4. فایل ZIP را انتخاب و نصب کنید

### 2. فعال‌سازی
1. به بخش "افزونه‌ها" در پنل وردپرس بروید
2. پلاگین "دایرکتوری آموزشی" را پیدا کنید
3. روی "فعال‌سازی" کلیک کنید

### 3. تنظیمات اولیه

پس از فعال‌سازی، پلاگین به صورت خودکار موارد زیر را انجام می‌دهد:

✅ ایجاد Custom Post Types:
- آموزشگاه‌ها (Academies)
- مدارس (Schools)
- معلمین (Teachers)

✅ ایجاد Taxonomies:
- رشته‌ها (Subjects)
- شهرها (Cities)
- مقاطع تحصیلی (Grade Levels)
- تخصص‌ها (Specialties)
- نوع موسسه (Institution Types)

✅ ایجاد صفحات پیش‌فرض:
- صفحه آموزشگاه‌ها
- صفحه مدارس
- صفحه معلمین

## پیکربندی

### 1. ایجاد دسته‌بندی‌ها

قبل از ایجاد محتوا، ابتدا دسته‌بندی‌ها را تعریف کنید:

#### شهرها:
```
مدارس > شهرها > افزودن شهر جدید
مثال: تهران، مشهد، اصفهان، شیراز، تبریز
```

#### رشته‌ها:
```
مدارس > رشته‌ها > افزودن رشته جدید
مثال:
- ریاضی
- فیزیک
- شیمی
- زبان انگلیسی
- ادبیات فارسی
```

#### مقاطع تحصیلی:
```
مدارس > مقاطع تحصیلی > افزودن مقطع جدید
مثال:
- پیش‌دبستانی
- ابتدایی
- متوسطه اول
- متوسطه دوم
- دانشگاهی
```

#### تخصص‌ها (برای معلمین):
```
معلمین > تخصص‌ها > افزودن تخصص جدید
مثال:
- معلم ریاضی
- معلم زبان
- مربی ورزش
- معلم موسیقی
```

### 2. ایجاد محتوا

#### ایجاد آموزشگاه:
1. آموزشگاه‌ها > افزودن آموزشگاه
2. عنوان: نام آموزشگاه
3. توضیحات: معرفی کامل آموزشگاه
4. تصویر شاخص: آپلود عکس آموزشگاه
5. پر کردن فیلدهای اطلاعات تماس
6. پر کردن فیلدهای اطلاعات مکانی
7. انتخاب امکانات
8. تعیین رتبه
9. انتخاب دسته‌بندی‌ها (شهر، رشته، مقطع)
10. انتشار

#### ایجاد مدرسه:
مشابه آموزشگاه با این تفاوت که می‌توانید نوع مدرسه را انتخاب کنید:
- دولتی
- خصوصی
- غیرانتفاعی
- نمونه دولتی

#### ایجاد معلم:
1. معلمین > افزودن معلم
2. عنوان: نام معلم
3. توضیحات: بیوگرافی و تجربیات
4. تصویر شاخص: عکس معلم
5. پر کردن اطلاعات معلم:
   - سابقه تدریس
   - مدرک تحصیلی
   - گواهینامه‌ها
   - اطلاعات تماس
   - هزینه تدریس
6. انتخاب تخصص‌ها
7. انتخاب آموزشگاه/مدرسه مرتبط
8. انتشار

### 3. نمایش در سایت

#### روش 1: استفاده از صفحات پیش‌فرض
صفحات زیر به صورت خودکار ایجاد شده‌اند:
- `/academies` - لیست آموزشگاه‌ها
- `/schools` - لیست مدارس
- `/teachers` - لیست معلمین

این صفحات را به منوی سایت اضافه کنید:
```
ظاهر > منوها > افزودن صفحه
```

#### روش 2: استفاده از Shortcodes
در هر صفحه یا پست می‌توانید این کدها را قرار دهید:

```
نمایش آموزشگاه‌ها:
[ed_academies]

نمایش مدارس:
[ed_schools]

نمایش معلمین:
[ed_teachers]

فرم جستجو:
[ed_search]
```

#### روش 3: استفاده از ویجت‌ها
1. ظاهر > ویجت‌ها
2. ویجت "متن" یا "HTML سفارشی" را اضافه کنید
3. Shortcode را در ویجت قرار دهید

### 4. تنظیمات پیشرفته

#### تغییر Permalink:
```
تنظیمات > پیوندهای یکتا > ذخیره تغییرات
(این کار rewrite rules را بازنویسی می‌کند)
```

#### تنظیم صفحه اصلی:
اگر می‌خواهید یکی از صفحات پیش‌فرض صفحه اصلی باشد:
```
تنظیمات > خواندن > صفحه نمایش داده شده در صفحه اصلی
```

## نکات مهم

### دسترسی‌ها
برای ایجاد و ویرایش محتوا، کاربران باید این دسترسی‌ها را داشته باشند:
- ویرایشگر (Editor) یا بالاتر
- نویسنده (Author) با دسترسی محدود

### بهینه‌سازی تصاویر
برای بهترین نمایش:
- حداقل عرض: 800 پیکسل
- نسبت تصویر: 4:3 یا 16:9
- فرمت: JPG یا PNG
- حجم: کمتر از 500 کیلوبایت

### توصیه‌های عملکرد
1. از کش استفاده کنید (مثل WP Super Cache)
2. تصاویر را بهینه کنید (مثل Smush)
3. از CDN استفاده کنید
4. دیتابیس را بهینه کنید

## عیب‌یابی

### مشکل: منوها در پنل مدیریت نمایش داده نمی‌شوند
**راه‌حل**: 
```
1. پلاگین را غیرفعال و دوباره فعال کنید
2. تنظیمات > پیوندهای یکتا > ذخیره تغییرات
```

### مشکل: صفحات 404 برمی‌گردانند
**راه‌حل**:
```
تنظیمات > پیوندهای یکتا > ذخیره تغییرات
```

### مشکل: استایل‌ها به درستی اعمال نمی‌شوند
**راه‌حل**:
```
1. کش مرورگر را پاک کنید
2. کش پلاگین را پاک کنید
3. بررسی کنید فایل‌های CSS به درستی بارگذاری می‌شوند
```

### مشکل: تصاویر نمایش داده نمی‌شوند
**راه‌حل**:
```
1. بررسی کنید فایل placeholder.jpg وجود دارد
2. مجوزهای پوشه uploads را بررسی کنید (755)
3. از آپلود مجدد تصاویر استفاده کنید
```

## پشتیبان‌گیری

قبل از هر به‌روزرسانی:
1. از دیتابیس پشتیبان بگیرید
2. از فایل‌های پلاگین پشتیبان بگیرید
3. تست کنید در یک محیط Staging

## حذف پلاگین

هنگام حذف پلاگین:
- تمام پست‌ها (آموزشگاه‌ها، مدارس، معلمین) **حذف نمی‌شوند**
- Taxonomies حذف می‌شوند
- صفحات پیش‌فرض **حذف نمی‌شوند** (باید دستی حذف شوند)

برای حذف کامل داده‌ها، قبل از حذف:
```php
// اضافه کردن این کد به functions.php قبل از حذف
function ed_delete_all_data() {
    // حذف تمام پست‌ها
    $post_types = array('academy', 'school', 'teacher');
    foreach ($post_types as $post_type) {
        $posts = get_posts(array('post_type' => $post_type, 'posts_per_page' => -1));
        foreach ($posts as $post) {
            wp_delete_post($post->ID, true);
        }
    }
}
add_action('admin_init', 'ed_delete_all_data');
```

## به‌روزرسانی

برای به‌روزرسانی پلاگین:
1. از فایل‌ها و دیتابیس پشتیبان بگیرید
2. فایل‌های جدید را جایگزین کنید
3. در صورت نیاز، به تنظیمات > پیوندهای یکتا بروید و ذخیره کنید
4. کش را پاک کنید

---

در صورت بروز مشکل، لطفا یک issue در GitHub ایجاد کنید.
