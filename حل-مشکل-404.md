# 🚨 حل مشکل صفحه 404 (پیدا نشد)

## مشکل:
وقتی روی آموزشگاه/معلم/مدرسه کلیک می‌کنید، صفحه "پیدا نشد" (404) نشان می‌دهد.

---

## ⚡ راه حل سریع (2 دقیقه):

### روش 1️⃣: استفاده از اسکریپت خودکار (پیشنهادی! 🔥)

```
1. فایل fix-404.php را آپلود کنید به:
   wp-content/plugins/educational-directory/fix-404.php

2. در مرورگر باز کنید:
   https://سایت-شما.com/wp-content/plugins/educational-directory/fix-404.php

3. صفحه سبز با پیام "انجام شد" می‌بینید ✅

4. فایل fix-404.php را پاک کنید (مهم!)

5. کش مرورگر را پاک کنید (Ctrl+F5)

6. یک آموزشگاه باز کنید - کار می‌کند! 🎉
```

---

### روش 2️⃣: دستی (اگر روش 1 کار نکرد)

#### قدم 1: Permalink را بازنویسی کنید
```
وردپرس ادمین
├─ تنظیمات
│  └─ پیوندهای یکتا
│     ├─ روی "نام نوشته" کلیک کنید
│     └─ دکمه "ذخیره تغییرات" را بزنید
│
└─ تمام! ✅
```

#### قدم 2: پلاگین را غیرفعال و فعال کنید
```
افزونه‌ها
├─ غیرفعال کردن Educational Directory
├─ صبر 2 ثانیه
└─ فعال کردن Educational Directory
```

#### قدم 3: کش را پاک کنید
```
✅ کش مرورگر: Ctrl + F5 (یا Cmd + Shift + R در مک)
✅ کش وردپرس: اگر پلاگین کش دارید، پاکش کنید
✅ کش سرور: اگر هاست شما کش دارد، از کنترل پنل پاکش کنید
```

#### قدم 4: تست کنید
```
1. به لیست آموزشگاه‌ها بروید
2. روی یک آموزشگاه کلیک کنید
3. باید صفحه زیبا را ببینید! 🎉
```

---

## 🔍 چرا این مشکل پیش آمد؟

WordPress برای هر Custom Post Type یک "Rewrite Rule" می‌سازد:

```
academy/نام-آموزشگاه     →  صفحه single-academy.php
school/نام-مدرسه         →  صفحه single-school.php
teacher/نام-معلم         →  صفحه single-teacher.php
```

این قوانین در دیتابیس ذخیره می‌شوند. وقتی پلاگین را:
- نصب می‌کنید
- فعال می‌کنید
- فایل‌هایش را تغییر می‌دهید

این قوانین باید **بازنویسی** شوند (flush rewrite rules).

---

## 🧪 تست: آیا مشکل حل شد؟

### تست 1: لینک مستقیم
```
1. به لیست آموزشگاه‌ها بروید
2. روی یک آموزشگاه کلیک کنید
3. آیا صفحه باز شد؟
   ✅ بله - عالی!
   ❌ خیر - روش 1 یا 2 را دوباره امتحان کنید
```

### تست 2: URL دستی
```
در مرورگر تایپ کنید:
https://سایت-شما.com/academy/test

اگر صفحه 404 نبود، یعنی Rewrite Rules کار می‌کند ✅
```

---

## 🛠️ اگر باز هم کار نکرد:

### چک 1: آیا تمپلیت‌ها موجود هستند؟
```
بروید به:
wp-content/plugins/educational-directory/templates/

باید این فایل‌ها را ببینید:
✅ single-academy.php
✅ single-school.php
✅ single-teacher.php
```

### چک 2: آیا CPT ها ثبت شدند؟
```
در ادمین وردپرس:
✅ منوی "آموزشگاه‌ها" را می‌بینید؟
✅ منوی "مدارس" را می‌بینید؟
✅ منوی "معلمین" را می‌بینید؟

اگر نه، یعنی پلاگین درست لود نشده!
```

### چک 3: آیا .htaccess درست است؟
```
فایل .htaccess در ریشه وردپرس باید این خطوط را داشته باشد:

# BEGIN WordPress
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]
</IfModule>
# END WordPress

اگر ندارد، از تنظیمات > پیوندهای یکتا > ذخیره تغییرات
وردپرس خودش اضافه می‌کند.
```

---

## 📞 راه حل‌های احتمالی دیگر:

### مشکل 1: تم شما با get_header() سازگار نیست
```
✅ راه حل: تمپلیت‌های ما از استایل inline استفاده می‌کنند
   باید کار کند حتی اگر تم شما ساده باشد
```

### مشکل 2: پلاگین دیگری دارد conflict می‌کند
```
✅ راه حل: تمام پلاگین‌ها را غیرفعال کنید
   فقط Educational Directory را فعال کنید
   تست کنید - اگر کار کرد، یکی یکی پلاگین‌ها را فعال کنید
```

### مشکل 3: سرور شما mod_rewrite ندارد
```
✅ راه حل: با هاست خود تماس بگیرید و بگویید:
   "لطفاً mod_rewrite را فعال کنید"
   یا در تنظیمات > پیوندهای یکتا > "ساده" را انتخاب کنید
```

---

## ✅ خلاصه (به فارسی ساده):

1. **فایل fix-404.php را آپلود و اجرا کنید** (سریع‌ترین راه)
2. **یا برید تنظیمات > پیوندهای یکتا > ذخیره** (دستی)
3. **کش مرورگر را پاک کنید** (Ctrl+F5)
4. **تست کنید** - باید کار کند! 🎉

---

## 🔥 نکته مهم:

**این مشکل فقط یکبار پیش می‌آید!**

بعد از فلاش کردن Permalink:
✅ تمام لینک‌ها کار می‌کنند
✅ دیگر نیازی به کار خاصی نیست
✅ می‌توانید fix-404.php را پاک کنید

---

**بعد از انجام این کارها بگویید کار کرد؟** 🚀
