# 🔧 راهنمای رفع مشکل کامل

## ❌ مشکل شما:
"پلاگین کار نمی‌کنه، فقط صفحات ساخته"

## ✅ تغییراتی که انجام دادم:

### 1️⃣ اصلاح فایل اصلی (`educational-directory.php`)
- ✅ ترتیب بارگذاری فایل‌ها را اصلاح کردم
- ✅ بررسی وجود کلاس‌ها را اضافه کردم  
- ✅ اولویت hook ها را تنظیم کردم
- ✅ مشکل activate را حل کردم

### 2️⃣ اصلاح فایل Post Types (`includes/class-post-types.php`)
- ✅ اولویت register را به 0 تغییر دادم

### 3️⃣ اصلاح فایل Taxonomies (`includes/class-taxonomies.php`)  
- ✅ اولویت register را به 5 تغییر دادم

### 4️⃣ ایجاد فایل دیباگ جدید (`debug-plugin.php`)
- 🆕 فایل تست کامل و حرفه‌ای

---

## 🚀 حالا چه کار باید کنید؟

### قدم 1️⃣: آپلود فایل‌های جدید

فایل‌های زیر را دوباره آپلود کنید (جایگزین فایل‌های قدیمی):

```
educational-directory/
├── educational-directory.php              ← اصلاح شده ✅
├── includes/
│   ├── class-post-types.php              ← اصلاح شده ✅
│   └── class-taxonomies.php              ← اصلاح شده ✅
└── debug-plugin.php                       ← جدید 🆕
```

### قدم 2️⃣: غیرفعال و فعال کردن پلاگین

```
1. به پنل وردپرس بروید
2. افزونه‌ها > افزونه‌های نصب شده
3. پلاگین "دایرکتوری آموزشی" را پیدا کنید
4. روی "غیرفعال" کلیک کنید
5. صبر کنید 2 ثانیه
6. روی "فعال‌سازی" کلیک کنید
```

### قدم 3️⃣: بازنویسی Permalink

```
1. تنظیمات > پیوندهای یکتا
2. هیچ تغییری ندهید!
3. فقط روی "ذخیره تغییرات" کلیک کنید
```

### قدم 4️⃣: استفاده از فایل دیباگ

در مرورگر به این آدرس بروید:
```
http://your-site.com/wp-content/plugins/educational-directory/debug-plugin.php
```

این فایل به شما می‌گه دقیقاً کجا مشکل است!

### قدم 5️⃣: بررسی نتیجه

حالا به پنل مدیریت برگردید. باید ببینید:

```
پیشخوان
نوشته‌ها
رسانه‌ها
صفحات
نظرها
🏢 آموزشگاه‌ها    ← باید ببینید!
🏫 مدارس          ← باید ببینید!
👨‍🏫 معلمین         ← باید ببینید!
ظاهر
افزونه‌ها
```

---

## 🧪 تست نهایی:

```
1. روی "آموزشگاه‌ها" کلیک کنید
2. "افزودن آموزشگاه" را انتخاب کنید
3. یک عنوان تستی بنویسید (مثلاً "تست")
4. روی "انتشار" کلیک کنید
```

✅ اگر ذخیره شد، تبریک! همه چیز درست شده! 🎉

---

## ❓ اگر باز هم کار نکرد؟

### بررسی 1: فایل debug را باز کنید
```
http://your-site.com/wp-content/plugins/educational-directory/debug-plugin.php
```
و ببینید چه چیزی قرمز (❌) است.

### بررسی 2: Debug Mode را فعال کنید

در فایل `wp-config.php` این خطوط را اضافه کنید:

```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

سپس:
1. پلاگین را غیرفعال کنید
2. پلاگین را فعال کنید
3. فایل `wp-content/debug.log` را باز کنید
4. خطاها را به من بفرستید

### بررسی 3: مطمئن شوید فایل‌های جدید آپلود شده‌اند

فایل `educational-directory.php` را باز کنید و ببینید آیا این خط را دارد:

```php
// راه‌اندازی اجزا - اولویت 0 برای اطمینان از اجرای اول
add_action('init', array($this, 'init_components'), 0);
```

✅ اگر دارید: فایل‌های جدید آپلود شده‌اند  
❌ اگر ندارید: دوباره فایل‌ها را آپلود کنید

---

## 📊 چک لیست نهایی:

قبل از اینکه بگویید کار نمی‌کند، این موارد را بررسی کنید:

- [ ] فایل‌های جدید را آپلود کردم
- [ ] فایل‌های قدیمی را جایگزین کردم (نه کنارشان!)
- [ ] پلاگین را غیرفعال و فعال کردم
- [ ] Permalink را بازنویسی کردم (تنظیمات > پیوندهای یکتا > ذخیره)
- [ ] فایل debug را باز کردم و بررسی کردم
- [ ] کش مرورگر را پاک کردم (Ctrl+F5)
- [ ] کش سرور را پاک کردم (اگر دارید)

---

## 📞 اگر همچنان مشکل دارید:

به من این اطلاعات را بدهید:

### 1. خروجی فایل debug:
```
http://your-site.com/wp-content/plugins/educational-directory/debug-plugin.php
```
عکس از صفحه بگیرید یا متن را کپی کنید

### 2. نسخه‌ها:
- نسخه وردپرس: (تنظیمات > درباره)
- نسخه PHP: (ابزارها > سلامت سایت > اطلاعات)

### 3. خطاها:
- محتوای `wp-content/debug.log` (اگر فعال کردید)
- یا عکس از خطا

### 4. بررسی:
- آیا منوها را می‌بینید؟ (آموزشگاه‌ها، مدارس، معلمین)
- آیا می‌توانید چیزی اضافه کنید؟
- آیا خطایی می‌گیرید؟

---

## 💡 نکات مهم:

### 🔥 نکته 1: حتماً فایل‌های جدید را آپلود کنید!
فایل‌هایی که اصلاح کردم:
- `educational-directory.php`
- `includes/class-post-types.php`
- `includes/class-taxonomies.php`

### 🔥 نکته 2: حتماً Permalink را بازنویسی کنید!
این خیلی مهمه! 90% مشکلات با این حل می‌شه.

### 🔥 نکته 3: از فایل debug استفاده کنید!
این فایل دقیقاً می‌گه کجا مشکل است.

---

## ✅ انتظار می‌رود بعد از این کارها:

1. ✅ منوهای آموزشگاه/مدرسه/معلم در پنل مدیریت ظاهر شوند
2. ✅ بتوانید آموزشگاه/مدرسه/معلم اضافه کنید
3. ✅ دسته‌بندی‌ها (شهرها، رشته‌ها) کار کنند
4. ✅ شورت کدها کار کنند
5. ✅ همه چیز درست کار کند!

---

**موفق باشید! 🚀**

اگر باز هم مشکل داشتید، خروجی فایل debug را به من نشون بدید تا کمکتون کنم.
